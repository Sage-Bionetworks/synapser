# synDeleteProvenance

Removes provenance information from an Entity and deletes the associated
Activity.

## Usage

``` r
synDeleteProvenance(entity)
```

## Arguments

- entity:

  An Entity or Synapse ID to modify

## Examples

``` r
if (FALSE) { # \dontrun{
synDeleteProvenance("syn11678572")
} # }
```
