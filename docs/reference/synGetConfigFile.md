# synGetConfigFile

Retrieves the client configuration information.

## Usage

``` r
synGetConfigFile(configPath)
```

## Arguments

- configPath:

  Path to configuration file on local file system

## Value

a RawConfigParser populated with properties from the user's
configuration file.
