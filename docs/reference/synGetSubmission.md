# synGetSubmission

Gets a Submission object by its id.

## Usage

``` r
synGetSubmission(id)
```

## Arguments

- id:

  The id of the submission to retrieve

## Value

a Submission object

## Examples

``` r
if (FALSE) { # \dontrun{
synGetSubmission(submissionId)
} # }
```
